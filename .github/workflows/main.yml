name: Security Check

on:
  push:
    branches:
      - main  # O el nombre del branch donde deseas que se ejecute
  pull_request:
    branches:
      - main  # Esto es opcional, pero útil si deseas ejecutar el workflow en PRs

jobs:
  security:
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Install dependencies
      run: npm install

    - name: Install yamllint
      run: |
        python3 -m pip install --upgrade pip
        python3 -m pip install yamllint

    - name: Run yamllint
      run: yamllint .github/workflows/*.yml
